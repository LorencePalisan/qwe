Add one or more icons to the project's icon barrel.

Icon name(s): $ARGUMENTS

Steps:
1. Open `src/icons/index.ts`
2. Add the named export(s) from `lucide-react` — keep the list alphabetically sorted within logical groups
3. Confirm the icon name exists in lucide-react before adding (icon names are PascalCase)

After adding, icons can be imported anywhere via `@/icons`.
